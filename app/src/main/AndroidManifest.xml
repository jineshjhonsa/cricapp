<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin.cricapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
     ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".add_Player"
            android:label="@string/title_activity_add__player"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".match_select"
            android:label="@string/title_activity_matches"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".final_tabs"
            android:label="@string/title_activity_final_tabs"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".innings_select"
            android:label="@string/title_activity_innings"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".first_select"
            android:label="@string/title_activity_first_select"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>